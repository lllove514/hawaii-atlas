# Rainfall Gradient Explorer — placeholder

Reserved slot for the fourth atlas map: the islands' extreme windward/leeward
rainfall gradients. The page at `index.html` is a styled "coming soon" card so
the hub link resolves; the pipeline will live in
`../data-pipelines/rainfall-gradient/` when the map lands.
