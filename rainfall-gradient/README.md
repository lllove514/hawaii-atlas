# Rainfall Gradient Explorer — placeholder

Reserved slot for the fourth atlas map, covering the islands' windward and
leeward rainfall gradients. The page at `index.html` is a styled "coming soon"
card so the hub link resolves. The pipeline will live in
`../data-pipelines/rainfall-gradient/` when the map lands.
